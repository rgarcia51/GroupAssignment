<%= form_with(model: disease_date, local: true) do |form| %>
  <% if disease_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(disease_date.errors.count, "error") %> prohibited this disease_date from being saved:</h2>

      <ul>
        <% disease_date.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="field">
    <%= form.label :animal_id %>
    <%= form.collection_select :animal_id, Animal.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :disease_id %>
    <%= form.collection_select :disease_id, Disease.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
