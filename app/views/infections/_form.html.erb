<%= form_with(model: infection, local: true) do |form| %>
  <% if infection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(infection.errors.count, "error") %> prohibited this infection from being saved:</h2>

      <ul>
        <% infection.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :animal_id %>
    <%= form.text_field :animal_id %>
  </div>

  <div class="field">
    <%= form.label :disease_id %>
    <%= form.text_field :disease_id %>
  </div>

  <div class="field">
    <%= form.label :infectionDate %>
    <%= form.date_select :infectionDate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
